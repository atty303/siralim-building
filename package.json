{
  "name": "siralim-building",
  "version": "1.0.0",
  "description": "# Running the app locally",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "concurrently \"npm run serve:tailwind\" \"npm run serve:dioxus\"",
    "serve:tailwind": "tailwindcss -i ./src/input.css -o ./public/tailwind.css --watch",
    "serve:dioxus": "dx serve --hot-reload"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "daisyui": "^3.5.0",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
